import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type'};
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});try{const body=await req.json();const supabase=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!);const {data,error}=await supabase.rpc('roleta_spin_event',{p_slug:body.eventSlug,p_name:body.name,p_whatsapp:body.whatsapp,p_instagram:body.instagram||null,p_email:body.email||null});if(error)throw error;return Response.json(data?.[0],{headers:cors})}catch(error){return Response.json({error:'Não foi possível realizar o giro.'},{status:400,headers:cors})}});
