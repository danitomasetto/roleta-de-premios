import { SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY } from './config.js';
const form=document.querySelector('#participantForm'), message=document.querySelector('#formMessage');
form.addEventListener('submit',async event=>{event.preventDefault();const eventSlug=new URLSearchParams(location.search).get('evento');if(!eventSlug){message.textContent='Este link de evento não é válido.';return}if(!SUPABASE_URL||!SUPABASE_PUBLISHABLE_KEY){message.textContent='A roleta ainda está sendo configurada.';return}const button=form.querySelector('button');button.disabled=true;message.textContent='Girando…';try{const fd=new FormData(form);const res=await fetch(`${SUPABASE_URL}/functions/v1/spin`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({eventSlug,name:fd.get('name'),whatsapp:fd.get('whatsapp'),instagram:fd.get('instagram'),email:fd.get('email')})});const result=await res.json();if(!res.ok)throw new Error(result.error);form.innerHTML=`<div class="result"><span>${result.prize_symbol}</span><h2>${result.prize_title}</h2>${result.coupon_code?`<code>${result.coupon_code}</code><p>Apresente este código à equipe.</p>`:'<p>Seu prêmio foi registrado. Procure a equipe para receber.</p>'}</div>`;message.textContent=''}catch(error){message.textContent='Não foi possível realizar o giro. Tente novamente.';button.disabled=false}});
