import { supabase } from './supabase.js';
const form=document.querySelector('#eventForm'),rouletteOptions=document.querySelector('#rouletteOptions'),message=document.querySelector('#eventMessage'),slugInput=document.querySelector('[name=slug]');const {data:{session}}=await supabase.auth.getSession();if(!session)location.href='login.html';
slugInput.closest('label').firstChild.textContent='Nome curto do link';slugInput.removeAttribute('pattern');const linkHelp=document.createElement('small');linkHelp.style.cssText='color:#80686a;font-weight:400;line-height:1.4';slugInput.insertAdjacentElement('afterend',linkHelp);function updateLinkHelp(){const value=slugInput.value.trim()||'nome-do-evento';linkHelp.textContent=`Use apenas letras, números e hífen. Ex.: feira-2026. Link: ${location.origin}${location.pathname.replace('novo-evento.html','')}participar.html?evento=${value}`;}slugInput.addEventListener('input',updateLinkHelp);updateLinkHelp();
document.querySelectorAll('[name=eventType]').forEach(input=>input.addEventListener('change',()=>rouletteOptions.hidden=document.querySelector('[name=eventType]:checked').value!=='roulette'));
form.addEventListener('submit',async e=>{e.preventDefault();const data=new FormData(form),title=data.get('title'),slug=data.get('slug').trim().toLowerCase(),eventType=data.get('eventType'),layout=eventType==='roulette'?data.get('layout'):'slots',palette=document.querySelector('[name=palette]:checked');if(!/^[a-z0-9-]{3,80}$/.test(slug)){message.textContent='No nome curto do link use somente letras, números e hífen. Ex.: evento3.';return}message.textContent='Criando…';const {data:id,error}=await supabase.rpc('roleta_create_event',{p_title:title,p_slug:slug,p_layout:layout,p_primary_color:palette?.dataset.primary||'#681D30',p_secondary_color:palette?.dataset.secondary||'#C79339',p_event_type:eventType});if(error){message.textContent='Não foi possível criar. Esse nome curto pode já estar em uso.';return}location.href=eventType==='list_draw'?`lista.html?event=${id}`:'./'});
