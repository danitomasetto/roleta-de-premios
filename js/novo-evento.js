import { supabase } from './supabase.js';
const form=document.querySelector('#eventForm'),rouletteOptions=document.querySelector('#rouletteOptions'),message=document.querySelector('#eventMessage');const {data:{session}}=await supabase.auth.getSession();if(!session)location.href='login.html';
document.querySelectorAll('[name=eventType]').forEach(input=>input.addEventListener('change',()=>rouletteOptions.hidden=document.querySelector('[name=eventType]:checked').value!=='roulette'));
form.addEventListener('submit',async e=>{e.preventDefault();const data=new FormData(form),title=data.get('title'),slug=data.get('slug'),eventType=data.get('eventType'),layout=eventType==='roulette'?data.get('layout'):'slots',palette=document.querySelector('[name=palette]:checked');message.textContent='Criando…';const {data:id,error}=await supabase.rpc('roleta_create_event',{p_title:title,p_slug:slug,p_layout:layout,p_primary_color:palette?.dataset.primary||'#681D30',p_secondary_color:palette?.dataset.secondary||'#C79339',p_event_type:eventType});if(error){message.textContent='Não foi possível criar. Confira o link informado.';return}location.href=eventType==='list_draw'?`lista.html?event=${id}`:'./'});
